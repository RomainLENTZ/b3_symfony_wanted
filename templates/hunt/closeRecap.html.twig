{% extends 'base.html.twig' %}

{% block title %}Hello HuntController!{% endblock %}

{% block body %}
    <div class="container">
        <h1>Cloturer la chasse : "{{ hunt.name }}" à bien étée cloturé</h1>
        <p><a href="{{ path('app_hunt_index_hunt') }}">Retourner à la liste de chasse</a> ou <a href="{{ path('app_hunt_add_hunt') }}">ajoutez en une nouvelle</a></p>
    </div>
{% endblock %}