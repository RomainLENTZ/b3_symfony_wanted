{% extends 'base.html.twig' %}

{% block title %}Hello HuntController!{% endblock %}

{% block body %}

    <div class="close-hunt-wrapper">
        <h1>La chasse "{{ hunt.name }}" est désormais clôturée</h1>

        <div class="button-wrapper">

            <a href="{{ path('app_hunt_index_hunt') }}">Retourner chasser</a>

            <a href="{{ path('app_hunt_add_hunt') }}">Ajouter une chasse</a>

        </div>

    </div>

{% endblock %}